import { StyleSheet } from "react-native";
import {
    windowHeight,
} from "../../../../theme/appConstant";

export default productDetailLoaderStyles = StyleSheet.create({
    imgLoader: {
        width: '100%',
        height: windowHeight(240),
    },
})