import { StyleSheet } from "react-native";
import { windowWidth } from "../../../../../../theme/appConstant";

export default sliderLoaderStyles = StyleSheet.create({
    view: {
        marginLeft: windowWidth(10)
    },
});